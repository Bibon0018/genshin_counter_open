import 'package:flutter/material.dart';

class FragmentsPickCard extends StatelessWidget {
  const FragmentsPickCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}